---
import CategoryBadge from './CategoryBadge.astro';
import ArticleMeta from './ArticleMeta.astro';
import TagBadge from './TagBadge.astro';
import { assetSrc } from '../utils/slug';

interface Props {
  title: string;
  excerpt: string;
  href: string;
  heroImage?: string;
  heroImageAlt?: string;
  category: string;
  pubDate: Date;
  author?: string;
  readingTime?: number;
  lang?: string;
  featured?: boolean;
  tags?: string[];
  seriesPosition?: { title: string; part: number; total: number };
}

const {
  title,
  excerpt,
  href,
  heroImage,
  heroImageAlt = '',
  category,
  pubDate,
  author = 'Norsk luftvern',
  readingTime,
  lang = 'no',
  tags,
  seriesPosition,
} = Astro.props;
---

<article class="article-card" data-article-lang={lang} data-article-date={pubDate.getTime().toString()} data-article-category={category} data-article-tags={tags?.join(',') ?? ''}>
  <a href={href} class="card-image-link" tabindex="-1" aria-hidden="true">
    {heroImage ? (
      <img
        src={assetSrc(heroImage)}
        alt={heroImageAlt}
        class="card-image"
        loading="lazy"
        decoding="async"
        width="800"
        height="450"
      />
    ) : (
      <div class="card-placeholder">
        <span>NL</span>
      </div>
    )}
    <div class="card-category">
      <CategoryBadge category={category} />
    </div>
    <div class="card-lang" aria-label={`Language: ${lang.toUpperCase()}`}>
      {lang.toUpperCase()}
    </div>
  </a>

  <div class="card-body">
    {seriesPosition && (
      <div class="card-series">
        <span class="card-series-name">{seriesPosition.title}</span>
        <span class="card-series-sep">·</span>
        <span class="card-series-pos">Del {seriesPosition.part} av {seriesPosition.total}</span>
      </div>
    )}
    <h2 class="card-title">
      <a href={href}>{title}</a>
    </h2>
    <p class="card-excerpt">{excerpt}</p>
    {tags && tags.length > 0 && (
      <div class="card-tags">
        {tags.slice(0, 3).map(tag => <TagBadge tag={tag} />)}
      </div>
    )}
    <ArticleMeta
      pubDate={pubDate}
      author={author}
      readingTime={readingTime}
      lang={lang}
    />
  </div>
</article>

<style>
  .article-card {
    display: flex;
    flex-direction: column;
    background: var(--color-bg);
    border-radius: var(--radius-md);
    overflow: hidden;
    transition: box-shadow 0.2s;
  }

  .article-card:hover {
    box-shadow: 0 6px 20px rgba(28, 8, 8, 0.10);
  }

  /* ── Image ── */
  .card-image-link {
    display: block;
    position: relative;
    aspect-ratio: 16 / 9;
    overflow: hidden;
    text-decoration: none;
    flex-shrink: 0;
  }

  .card-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.35s ease;
  }

  .article-card:hover .card-image { transform: scale(1.03); }

  .card-placeholder {
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, var(--color-heading) 0%, #3D1515 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-display);
    font-size: 2rem;
    font-weight: 800;
    color: rgba(255,255,255,0.15);
    letter-spacing: 0.2em;
  }

  .card-category {
    position: absolute;
    top: var(--spacing-sm);
    left: var(--spacing-sm);
  }

  .card-lang {
    position: absolute;
    top: var(--spacing-sm);
    right: var(--spacing-sm);
    background: rgba(10, 3, 3, 0.55);
    color: rgba(255, 255, 255, 0.9);
    font-family: var(--font-ui);
    font-size: 0.6rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    padding: 0.15em 0.5em;
    border-radius: var(--radius-full);
    line-height: 1.6;
    pointer-events: none;
  }

  /* ── Body ── */
  .card-body {
    padding: var(--spacing-sm) var(--spacing-md) var(--spacing-md);
    display: flex;
    flex-direction: column;
    gap: var(--spacing-sm);
    flex: 1;
  }

  .card-title {
    font-family: var(--font-display);
    font-size: 1.05rem;
    font-weight: 800;
    line-height: 1.2;
    letter-spacing: 0.02em;
    text-transform: uppercase;
    color: var(--color-heading);
    margin: 0;
  }

  .card-title a {
    color: inherit;
    text-decoration: none;
    transition: color 0.15s;
  }

  .card-title a:hover { color: var(--color-navy); }

  .card-excerpt {
    font-family: var(--font-body);
    font-size: 0.9rem;
    line-height: 1.6;
    color: var(--color-muted);
    margin: 0;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .card-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.3em;
    margin-top: -0.15em;
  }

  .card-series {
    display: flex;
    align-items: center;
    gap: 0.35em;
    font-family: var(--font-ui);
    font-size: 0.65rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--color-teal);
    margin-bottom: -0.2em;
  }

  .card-series-sep { opacity: 0.4; }
  .card-series-pos { opacity: 0.75; }
</style>
